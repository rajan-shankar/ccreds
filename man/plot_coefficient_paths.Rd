% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot.R
\name{plot_coefficient_paths}
\alias{plot_coefficient_paths}
\title{Plot coefficient paths}
\usage{
plot_coefficient_paths(
  results,
  component = c("beta", "gamma"),
  lambda_fixed = NULL,
  feature_names = NULL,
  highlight = TRUE
)
}
\arguments{
\item{results}{A tibble returned by \code{\link[=ccreds]{ccreds()}}.}

\item{component}{Either \code{"beta"} or \code{"gamma"}.}

\item{lambda_fixed}{The value of the held-fixed penalty. If
\code{NULL} (default), uses the value at the minimum \code{CV_MAE_y}
row.}

\item{feature_names}{Optional character vector of length
\eqn{p} giving display names for the features. If \code{NULL},
uses \code{z1}, \code{z2}, etc.}

\item{highlight}{Logical; if \code{TRUE} (default), highlights
features that are non-zero at the minimum-CV model and adds
a dashed vertical line at the chosen penalty value.}
}
\value{
A \code{ggplot} object.
}
\description{
Shows how the estimated feature coefficients
\eqn{\boldsymbol{\beta}} or \eqn{\boldsymbol{\gamma}} change
as the corresponding penalty parameter varies. For
\code{component = "beta"}, the plot varies \eqn{\lambda_1} while
holding \eqn{\lambda_2} fixed, and vice versa for
\code{component = "gamma"}.
}
\examples{
\dontrun{
results <- ccreds(data, lambda_1s, lambda_2s)
plot_coefficient_paths(results, "beta")
plot_coefficient_paths(results, "gamma",
  feature_names = c("age", "weight", "height")
)
}

}
