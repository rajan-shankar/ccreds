% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot.R
\name{plot_density}
\alias{plot_density}
\title{Plot predictive densities}
\usage{
plot_density(
  fit,
  z,
  x_range = NULL,
  y_range = NULL,
  y_given = NULL,
  n_grid = 200
)
}
\arguments{
\item{fit}{A single fitted model list (e.g. one element of the
\code{full_fit} column from \code{\link[=ccreds]{ccreds()}} output), containing
\code{beta_0}, \code{alpha}, \code{beta}, \code{sigma_sq}, \code{gamma_0}, \code{gamma}.}

\item{z}{Numeric vector of feature values (length \eqn{p}).}

\item{x_range}{Numeric vector of length 2 giving the range
of \eqn{x} values to plot. Default is computed from the
chi-squared distribution implied by the model.}

\item{y_range}{Numeric vector of length 2 giving the range
of \eqn{y} values (used for the joint contour plot).
Default is computed from the model.}

\item{y_given}{Optional scalar. If supplied, produces a
density overlay of \eqn{f(x \mid \mathbf{z})} and
\eqn{f(x \mid \mathbf{z}, y = y_{given})} instead of the
joint contour.}

\item{n_grid}{Number of grid points per axis. Default is 200.}
}
\value{
A \code{ggplot} object.
}
\description{
Plots the joint density \eqn{f(y, x \mid \mathbf{z})} as a
filled contour, or, when \code{y_given} is supplied, overlays the
marginal density \eqn{f(x \mid \mathbf{z})} and the
conditional density \eqn{f(x \mid \mathbf{z}, y)} for a
specific value of \eqn{y}.
}
\examples{
\dontrun{
results <- ccreds(data, lambda_1s, lambda_2s)
fit <- results$full_fit[[1]]
z_vals <- c(0.5, -0.3, 1.2)

# Joint density contour
plot_density(fit, z_vals)

# Marginal vs conditional density of x given y = 60
plot_density(fit, z_vals, y_given = 60)
}

}
